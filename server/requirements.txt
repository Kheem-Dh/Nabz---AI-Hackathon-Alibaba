fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-dotenv==1.0.1
openai==1.59.6
httpx==0.28.1
python-multipart==0.0.20
SQLAlchemy==2.0.36
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # passlib 1.7.4 is incompatible with bcrypt>=4.1 / 5.x
PyJWT==2.10.1
gTTS>=2.5,<3       # fallback voice + non-Urdu languages
# torch + transformers are NOT installed by default. They are only needed for
# the legacy offline MMS voice (NABZ_TTS_ENGINE=mms), which is opt-in and is
# never reached by the default engine chain (Qwen3.5-Omni -> gTTS). Installing
# them added ~865 MB to the image, and on a spun-down free instance that pull
# was the difference between a ~35 s and a ~152 s cold start. To use the MMS
# voice: pip install -r requirements-mms.txt
pymupdf>=1.24,<2   # PDF → PNG for lab/prescription uploads (pip-only, no system deps)
boto3>=1.35,<2     # optional private S3 / Cloudflare R2 medical-document storage
pytest==8.3.4
alembic>=1.13,<2
